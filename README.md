# Mommy Platform - Модульная платформа для Telegram ботов

## 🚀 Обзор

**Mommy Platform** - это мощная модульная платформа для создания и управления Telegram ботами на основе aiogram. Платформа предоставляет четкую архитектуру с разделением ответственности между компонентами, что позволяет быстро создавать, поддерживать и масштабировать ботов.

### Ключевые особенности:
- **Модульная архитектура** - Каждый бот как отдельный модуль
- **Factory Pattern** - Регистрация ботов через фабричные функции
- **Simple Container** - Упрощенный контейнер для создания компонентов
- **Registry Pattern** - Централизованное управление ботами
- **Готовые шаблоны кнопок** - Переиспользуемые клавиатуры
- **База данных PostgreSQL** - Асинхронная работа с БД через SQLAlchemy
- **Миграции Alembic** - Управление схемой базы данных

## 📚 Документация

### Основная документация
- [📖 Полная документация платформы](docs/README.md) - Обзор архитектуры и компонентов
- [🛠️ Создание нового бота](docs/создание-бота.md) - Пошаговое руководство с диаграммами
- [📋 Шаблон для быстрого создания бота](docs/шаблон-бота.md) - Готовые шаблоны кода
- [⌨️ Система шаблонов кнопок](docs/шаблоны-кнопок.md) - Переиспользуемые клавиатуры
- [📋 История изменений](CHANGELOG.md) - Changelog проекта с версиями

### Архитектурные компоненты
- **BotRegistry** ([`core/registry.py`](core/registry.py:1)) - Центральный реестр ботов
- **BotContainer** ([`core/bot_container.py`](core/bot_container.py:1)) - DI контейнер
- **BotClient** ([`core/base/client.py`](core/base/client.py:1)) - Абстракция клиента
- **BotRouter** ([`core/base/router.py`](core/base/router.py:1)) - Маршрутизатор команд
- **BotService** ([`core/base/service.py`](core/base/service.py:1)) - Бизнес-логика

## 🏗️ Структура проекта

```
hope_platform/
├── bots/                    # Модули ботов
│   └── srv_servicebot/     # Пример бота
├── core/                   # Ядро платформы
│   ├── base/              # Базовые классы
│   ├── clients/           # Реализации клиентов
│   ├── database.py        # Конфигурация базы данных
│   ├── keyboard_templates/ # Шаблоны кнопок
│   ├── models/            # Модели данных
│   │   └── db/           # Модели базы данных
│   ├── repositories/      # Репозитории для работы с БД
│   └── schemas/           # Схемы данных
├── docs/                  # Документация
├── utils/                 # Утилиты и конфигурация
└── tests/                 # Тесты
```

## ⚡ Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Настройка базы данных
Убедитесь, что PostgreSQL установлен и запущен. Создайте базу данных:
```sql
CREATE DATABASE hope_platform;
```

### 3. Настройка конфигурации
Скопируйте `.env.example` в `.env` и настройте параметры:
```bash
cp .env.example .env
```
Отредактируйте `.env` файл, указав правильные параметры подключения к БД.

### 4. Применение миграций
```bash
alembic upgrade head
```

### 5. Создание нового бота
Следуйте инструкции в [создание-бота.md](docs/создание-бота.md) или используйте готовый [шаблон](docs/шаблон-бота.md).

### 6. Запуск платформы
```bash
python main.py
```

## 🎯 Пример использования

### Создание простого бота
```python
# bots/my_bot/service.py
from core.base.service import BotService

class MyBotService(BotService):
    async def start_command(self):
        return "Добро пожаловать в мой бот!"
```

### Использование шаблонов кнопок
```python
from core.keyboard_templates import SimpleTemplate

main_menu = SimpleTemplate([
    {"text": "🚀 Старт", "callback_data": "start"},
    {"text": "⚙️ Настройки", "callback_data": "settings"}
])
```

## 🔧 Конфигурация

Основные настройки в [`utils/config.py`](utils/config.py:5):
```python
class Config(BaseSettings):
    # Токены ботов
    SRV_SERVICEBOT_TOKEN: SecretStr

    # Настройки базы данных
    DB_HOST: str
    DB_PORT: str = "5432"
    DB_NAME: str
    DB_USER: str
    DB_PASSWORD: SecretStr
    DB_ENGINE: str = "postgresql"
    DB_DEV_MODE: Optional[bool] = True

    def get_db_url(self):
        return f"{self.DB_ENGINE}://{self.DB_USER}:{self.DB_PASSWORD.get_secret_value()}@{self.DB_HOST}:{self.DB_PORT}/{self.DB_NAME}"
```

## 📊 Мониторинг и логирование

Логи сохраняются в папке `logs/` с ротацией по дням. Используйте встроенное логирование:

```python
from utils.logger import logger

logger.info("Сообщение для логирования")
```

## 🧪 Тестирование

Запуск тестов:
```bash
python -m pytest tests/
```

## 🗄️ Работа с базой данных

Платформа использует PostgreSQL с SQLAlchemy для асинхронной работы с БД:

### Модели данных
Модели находятся в [`core/models/db/`](core/models/db/):
```python
class User(Base):
    __tablename__ = "users"
    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(255), nullable=False)
    lang = Column(String(10), default="en")
    created_at = Column(DateTime(timezone=True), server_default=func.now())
```

### Репозитории
Репозитории для работы с БД в [`core/repositories/`](core/repositories/):
```python
class UserRepository:
    async def create_user(self, *, user_id: int, name: str, lang: str) -> User:
        user = User(id=user_id, name=name, lang=lang)
        self.session.add(user)
        await self.session.flush()
        return user
```

### Миграции
Используйте Alembic для управления схемой БД:
```bash
# Создание новой миграции
alembic revision --autogenerate -m "Описание изменений"

# Применение миграций
alembic upgrade head

# Откат миграции
alembic downgrade -1
```

## 🤝 Вклад в проект

Приветствуются предложения и улучшения! Для внесения изменений:

1. Создайте ветку для новой функциональности
2. Добавьте тесты для нового кода
3. Обновите документацию
4. Создайте pull request

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел "Возможные проблемы и их решения" в [основной документации](docs/создание-бота.md#возможные-проблемы-и-их-решения)
2. Убедитесь в правильности конфигурации
3. Проверьте логи приложения

## 📄 Лицензия

Проект распространяется под лицензией MIT.

---

*Документация обновлена: 2026-01-29*
*Mommy Platform v1.0 - Модульная платформа для Telegram ботов*
