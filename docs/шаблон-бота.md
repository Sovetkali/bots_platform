# Шаблон для быстрого создания бота

Этот файл содержит готовый шаблон для быстрого создания нового бота в Mommy Platform. Скопируйте и адаптируйте код под свои нужды.

## Структура файлов

### 1. service.py - Бизнес-логика

```python
from core.base.service import BotService
from utils.logger import logger

class НовыйБотService(BotService):
    """
    Сервис для бизнес-логики нового бота.
    Реализуйте здесь всю основную логику работы бота.
    """

    async def start_command(self) -> str:
        """Обработка команды /start"""
        logger.info("Обработка команды /start")
        return "Добро пожаловать в НовыйБот! 🚀"

    async def help_command(self) -> str:
        """Обработка команды /help"""
        return """
Доступные команды:
/start - Начать работу
/help - Помощь

Просто отправьте сообщение, и бот ответит вам!
"""

    async def process_message(self, text: str) -> str:
        """Обработка текстовых сообщений"""
        logger.info(f"Обработка сообщения: {text}")

        # Пример простой логики - можно заменить на свою
        if "привет" in text.lower():
            return "Привет! Как дела?"
        elif "пока" in text.lower():
            return "До свидания! Возвращайтесь скорее!"
        else:
            return f"Вы сказали: '{text}'. Интересно!"

    async def handle_error(self, error: Exception) -> str:
        """Обработка ошибок"""
        logger.error(f"Ошибка в сервисе: {error}")
        return "Произошла ошибка. Попробуйте позже."
```

### 2. router.py - Маршрутизатор

```python
from aiogram import Router
from aiogram.types import Message
from aiogram.filters import Command, Text
from core.base.router import BotRouter
from .service import НовыйБотService
from utils.logger import logger

class НовыйБотRouter(BotRouter):
    """
    Маршрутизатор для обработки команд и сообщений.
    Регистрирует хендлеры и связывает их с сервисом.
    """

    def __init__(self, service: НовыйБотService):
        self._router = Router()
        self._service = service
        self._register_handlers()
        logger.info("Маршрутизатор НовыйБот инициализирован")

    def _register_handlers(self):
        """Регистрация всех хендлеров"""
        # Команда /start
        self._router.message.register(self.start, Command("start"))

        # Команда /help
        self._router.message.register(self.help, Command("help"))

        # Обработка текстовых сообщений
        self._router.message.register(self.handle_message, Text())

        # Обработка любых сообщений (fallback)
        self._router.message.register(self.fallback)

    @property
    def router(self) -> Router:
        return self._router

    async def start(self, message: Message):
        """Обработка команды /start"""
        try:
            text = await self._service.start_command()
            await message.answer(text)
        except Exception as e:
            error_text = await self._service.handle_error(e)
            await message.answer(error_text)

    async def help(self, message: Message):
        """Обработка команды /help"""
        try:
            text = await self._service.help_command()
            await message.answer(text)
        except Exception as e:
            error_text = await self._service.handle_error(e)
            await message.answer(error_text)

    async def handle_message(self, message: Message):
        """Обработка текстовых сообщений"""
        try:
            text = await self._service.process_message(message.text)
            await message.answer(text)
        except Exception as e:
            error_text = await self._service.handle_error(e)
            await message.answer(error_text)

    async def fallback(self, message: Message):
        """Fallback хендлер для неподдерживаемых типов сообщений"""
        await message.answer("Извините, я пока не умею обрабатывать этот тип сообщений.")
```

### 3. client.py - Клиент бота

```python
from core.clients.aiogram_client import AiogramBotClient
from core.models.client_config import BotClientConfig
from core.models.bot_mode import BotMode
from aiogram import Router
from utils.config import config
from utils.logger import logger

class НовыйБотClient(AiogramBotClient):
    """
    Клиент для нового бота.
    Наследует базовый aiogram клиент и добавляет специфичную конфигурацию.
    """

    name = "новый_бот"  # Уникальное имя бота (латинскими буквами, без пробелов)

    def __init__(self, router: Router):
        # Получаем токен из конфигурации
        # ВАЖНО: Добавьте НОВЫЙ_БОТ_TOKEN в config.py и .env
        token = config.НОВЫЙ_БОТ_TOKEN.get_secret_value()

        super().__init__(
            name=self.name,
            config=BotClientConfig(
                token=token,
                mode=BotMode.POLLING,  # Используйте POLLING для разработки
                webhook_url=None       # Для production можно настроить webhook
            )
        )

        # Включаем роутер в диспетчер
        self.dp.include_router(router)
        logger.info(f"Клиент бота '{self.name}' инициализирован")

    async def start(self) -> None:
        """Переопределение метода start для дополнительного логирования"""
        logger.info(f"Запуск бота '{self.name}'")
        await super().start()

    async def stop(self) -> None:
        """Переопределение метода stop для дополнительного логирования"""
        logger.info(f"Остановка бота '{self.name}'")
        await super().stop()
```

### 4. Конфигурация бота

Создайте или добавьте в [`utils/bots_config.py`](utils/bots_config.py:1):
```python
from bots.новый_бот.service import НовыйБотService
from bots.новый_бот.router import НовыйБотRouter
from bots.новый_бот.client import НовыйБотClient

BOT_CONFIGS = {
    'новый_бот': {
        'name': 'новый_бот',
        'service_class': НовыйБотService,
        'router_class': НовыйБотRouter,
        'client_class': НовыйБотClient,
        'dependencies': {}  # Дополнительные зависимости
    }
}
```

### 5. __init__.py - Регистрация бота

```python
from core.registry import register_bot
from core.bot_container import container
from utils.bots_config import BOT_CONFIGS
from utils.logger import logger

# Регистрируем конфигурацию в контейнере
container.register_bot(BOT_CONFIGS['новый_бот']['name'], BOT_CONFIGS['новый_бот'])

def create_bot():
    """
    Фабричная функция для создания экземпляра бота.
    Использует контейнер для создания компонентов.
    """
    logger.info("Создание экземпляра НовыйБот через контейнер")

    # Контейнер создаёт бота по конфигурации
    client = container.get_bot(BOT_CONFIGS['новый_бот']['name'])

    logger.info("Экземпляр НовыйБот создан успешно")
    return client

# Регистрируем фабрику в платформе
register_bot.register(BOT_CONFIGS['новый_бот']['name'], create_bot)
```

## Настройка конфигурации

### 1. Добавьте в utils/config.py

```python
class Config(BaseSettings):
    # ... существующие настройки

    # Добавьте эту строку:
    НОВЫЙ_БОТ_TOKEN: SecretStr
```

### 2. Добавьте в .env

```
НОВЫЙ_БОТ_TOKEN=your_actual_bot_token_here
```

### 3. Убедитесь, что существует utils/bots_config.py

Если файла нет, создайте его:
```python
# utils/bots_config.py
BOT_CONFIGS = {
    # Конфигурации всех ботов будут здесь
}
```

## Быстрые команды для копирования

### Для быстрого создания папки бота:

```bash
# Создайте структуру папок
mkdir -p bots/новый_бот

# Создайте файлы
touch bots/новый_бот/__init__.py
touch bots/новый_бот/client.py
touch bots/новый_бот/router.py
touch bots/новый_бот/service.py
```

### Для замены имен:

Замените во всех файлах:
- `НовыйБот` на ваше название класса (в CamelCase)
- `новый_бот` на ваше имя бота (в snake_case)

## Проверка работоспособности

После создания бота запустите платформу:

```bash
python main.py
```

В логах должны появиться сообщения:
- "Bot registered: новый_бот"
- "Initializing bot: новый_бот"
- "Starting bot: новый_бот"

## Дальнейшие шаги

1. **Настройте бизнес-логику** в `service.py`
2. **Добавьте новые команды** в `router.py`
3. **Настройте конфигурацию** для production
4. **Добавьте middleware** при необходимости
5. **Создайте тесты** для вашего бота

---
*Шаблон создан: 2026-01-27*
*Обновлено с упрощенным контейнером: 2026-01-29*
