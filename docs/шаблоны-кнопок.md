# Система шаблонов кнопок для ботов

## Обзор
Система шаблонов позволяет создавать переиспользуемые клавиатуры для Telegram ботов с минимальным кодом. Шаблоны обеспечивают простоту, гибкость и повторное использование между разными ботами.

## Быстрый старт

### Установка
```python
from core.keyboard_templates import SimpleTemplate, GridTemplate, MenuTemplate, ActionTemplate
```

### Простой пример
```python
# Создание шаблона главного меню
main_menu = SimpleTemplate([
    {"text": "🚀 Старт", "callback_data": "start"},
    {"text": "⚙️ Настройки", "callback_data": "settings"},
    {"text": "❓ Помощь", "callback_data": "help"}
])

# Использование в сервисе
keyboard = main_menu.build()
```

## Типы шаблонов

### 1. SimpleTemplate - Простые кнопки в один ряд
```python
template = SimpleTemplate([
    {"text": "Кнопка 1", "callback_data": "btn1"},
    {"text": "Кнопка 2", "callback_data": "btn2"},
    {"text": "Кнопка с URL", "url": "https://example.com"}
])

# Результат:
# [Кнопка 1]
# [Кнопка 2]
# [Кнопка с URL]
```

### 2. GridTemplate - Сетка кнопок
```python
template = GridTemplate(
    columns=2,
    buttons=[
        {"text": "1", "callback_data": "1"},
        {"text": "2", "callback_data": "2"},
        {"text": "3", "callback_data": "3"},
        {"text": "4", "callback_data": "4"}
    ]
)

# Результат:
# [1] [2]
# [3] [4]
```

### 3. MenuTemplate - Многоуровневое меню
```python
template = MenuTemplate(
    title="Главное меню",
    items=[
        {"text": "Профиль", "callback_data": "profile"},
        {"text": "Настройки", "callback_data": "settings"},
        {"text": "Помощь", "callback_data": "help"}
    ],
    back_button={"text": "← Назад", "callback_data": "back"}
)

# Результат:
# [Главное меню]
# [Профиль]
# [Настройки]
# [Помощь]
# [← Назад]
```

### 4. ActionTemplate - Кнопки действий
```python
template = ActionTemplate(
    confirm_text="✅ Подтвердить",
    cancel_text="❌ Отменить",
    confirm_data="confirm",
    cancel_data="cancel"
)

# Результат:
# [✅ Подтвердить] [❌ Отменить]
```

## Использование в ботах

### Интеграция с сервисом бота
```python
from core.keyboard_templates import SimpleTemplate, MenuTemplate

class MyBotService(BotService):
    def __init__(self):
        self.main_menu = SimpleTemplate([
            {"text": "🚀 Старт", "callback_data": "start"},
            {"text": "⚙️ Настройки", "callback_data": "settings"}
        ])

        self.settings_menu = MenuTemplate(
            items=[
                {"text": "Уведомления", "callback_data": "notifications"},
                {"text": "Язык", "callback_data": "language"}
            ],
            back_button={"text": "← Назад", "callback_data": "main"}
        )

    async def get_keyboard(self, menu_type: str = "main", **context):
        if menu_type == "main":
            return self.main_menu.build(**context)
        elif menu_type == "settings":
            return self.settings_menu.build(**context)
```

### Динамические кнопки с контекстом
```python
# Кнопки с подстановкой значений
template = SimpleTemplate([
    {"text": "Привет, {user_name}!", "callback_data": "greet_{user_id}"},
    {"text": "Баланс: {balance} руб.", "callback_data": "balance"}
])

# Использование с контекстом
keyboard = template.build(user_name="Иван", user_id=123, balance=1000)

# Результат:
# [Привет, Иван!] -> callback_data: "greet_123"
# [Баланс: 1000 руб.] -> callback_data: "balance"
```

## Пример из srv_servicebot

### Сервис бота
```python
class ServiceBotService(BotService):
    def __init__(self):
        self.main_menu = SimpleTemplate([
            {"text": "🚀 Старт", "callback_data": "start"},
            {"text": "⚙️ Настройки", "callback_data": "settings:open"},
            {"text": "❓ Помощь", "callback_data": "help"}
        ])

    async def get_keyboard(self, menu_type: str = "main", **context):
        return self.main_menu.build(**context)
```

### Роутер бота
```python
async def start(self, message: Message):
    user = UserContext(id=message.from_user.id, name=message.from_user.first_name)
    welcome_text = await self._service.start(user)
    keyboard = await self._service.get_keyboard("main", user_name=user.name)
    await message.answer(welcome_text, reply_markup=keyboard)
```

## Преимущества системы

### 1. Переиспользование
Один шаблон можно использовать в нескольких ботах:
```python
# shared_templates/main_menu.py
from core.keyboard_templates import SimpleTemplate

MAIN_MENU = SimpleTemplate([
    {"text": "Старт", "callback_data": "start"},
    {"text": "Настройки", "callback_data": "settings"}
])

# Бот 1
from shared_templates.main_menu import MAIN_MENU

# Бот 2
from shared_templates.main_menu import MAIN_MENU
```

### 2. Простота
Минимальный код для создания сложных клавиатур:
```python
# Старый способ (15+ строк)
class MainKeyboard(Keyboard):
    def build(self):
        return InlineKeyboardMarkup(inline_keyboard=[
            [InlineKeyboardButton(text="Старт", callback_data="start")],
            [InlineKeyboardButton(text="Настройки", callback_data="settings")]
        ])

# Новый способ (3 строки)
main_menu = SimpleTemplate([
    {"text": "Старт", "callback_data": "start"},
    {"text": "Настройки", "callback_data": "settings"}
])
```

### 3. Гибкость
Легкое изменение структуры без переписывания кода:
```python
# Быстро изменить сетку с 2 на 3 колонки
template = GridTemplate(columns=3, buttons=buttons)
```

### 4. Динамичность
Поддержка контекстных переменных:
```python
template.build(user_name="Иван", balance=1000, status="active")
```

## Создание собственных шаблонов

### Базовый класс
```python
from core.keyboard_templates.base import BaseKeyboardTemplate

class MyTemplate(BaseKeyboardTemplate):
    def build(self, **context) -> InlineKeyboardMarkup:
        # Ваша логика создания кнопок
        keyboard = []
        # ... создание кнопок
        return InlineKeyboardMarkup(inline_keyboard=keyboard)
```

## Миграция с существующего кода

### Старый способ
```python
from .keyboards.main import MainKeyboard

class ServiceBotService(BotService):
    async def get_keyboard(self):
        return MainKeyboard().build()
```

### Новый способ
```python
from core.keyboard_templates import SimpleTemplate

class ServiceBotService(BotService):
    def __init__(self):
        self.main_menu = SimpleTemplate([
            {"text": "Старт", "callback_data": "start"},
            {"text": "Настройки", "callback_data": "settings"}
        ])

    async def get_keyboard(self, **context):
        return self.main_menu.build(**context)
```

## Заключение

Система шаблонов кнопок предоставляет:
- **Простое API** для создания клавиатур
- **Переиспользование** между ботами
- **Гибкость** в изменении структуры
- **Динамические** возможности с контекстом
- **Совместимость** с существующим кодом

Для начала использования достаточно импортировать нужные шаблоны и создать экземпляры с конфигурацией кнопок.

---
*Документация проверена: 2026-02-04*
*Система шаблонов кнопок полностью актуальна*
